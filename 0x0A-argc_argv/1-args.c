#include <stdio.h>

/**
 * main - prints no of arg
 * @argv: a value
 * @argc: an input
 * Return: returns 0
 */

int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc);
		return (0);
}
