#include <stdio.h>

/**
 * main - prints name
 * @argc: a variable
 * @argv: an array
 * Return: returns 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
