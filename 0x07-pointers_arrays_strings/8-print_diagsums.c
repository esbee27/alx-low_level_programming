#include <stdio.h>
#include "main.h"

/**
 * print_diagsums - prints diag sums
 * @a: input
 * @size: a value
 * Return: returns o
 */

void print_diagsums(int *a, int size)
{
	int i, n,sum1 = 0, sum2 = 0;

	for (i 
